services:
  - type: web
    name: hellowork-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
